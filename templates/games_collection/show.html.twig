{% extends 'base.html.twig' %}

{% block title %}GamesCollection{% endblock %}

{% block body %}
    <h1>GamesCollection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom de la collection</th>
                <td>{{ games_collection.name }}</td>
            </tr>
            <tr>
                <th>Crée le</th>
                <td>{{ games_collection.createdAt ? games_collection.createdAt|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_games_collection_index') }}">back to list</a>

    <a href="{{ path('app_games_collection_edit', {'id': games_collection.id}) }}">edit</a>

    {{ include('games_collection/_delete_form.html.twig') }}
{% endblock %}

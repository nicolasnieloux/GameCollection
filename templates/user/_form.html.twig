{{ form_start(form) }}

    {{ form_label(form.pseudo, 'Pseudo',
        {'label_attr': {'class': 'm-3'}}
    ) }}
    {{ form_errors(form.pseudo) }}

    {{ form_widget(form.pseudo,
        {'attr': { 'class': 'form-control', 'required': true, 'autofocus': true }}
    ) }}

    {{ form_label(form.email, 'Email',
        {'label_attr': {'class': 'm-3'}}
    ) }}
    {{ form_errors(form.email) }}
    {{ form_widget(form.email,
        {'attr': { 'class': 'form-control', 'autocomplete': 'email', 'required': true, 'autofocus': true }}
    ) }}

    {{ form_label(form.password, 'Password',
        {'label_attr': {'class': 'm-3'}}
    ) }}
    {{ form_errors(form.password) }}
    {{ form_widget(form.password,
        {'attr': { 'class': 'form-control', 'autocomplete': 'current-password', 'required': true }}
    ) }}

<button class="btn btn-lg btn-primary">{{ button_label|default('Create') }}</button>

{{ form_end(form) }}